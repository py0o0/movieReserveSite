<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.moviecommu.repository.FollowingRepository">
   <insert id="save" parameterType="Flw">
       insert into flw_table(userId,flwName)
       values(#{userId},#{flwName})
   </insert>

    <select id="findByFlw" parameterType="Flw" resultType="Integer">
        select count(*) from flw_table where userId=#{userId} and flwName=#{flwName}
    </select>

    <delete id="deleteByFlw" parameterType="Flw">
        delete from flw_table where userId=#{userId} and flwName=#{flwName}
    </delete>

    <select id="flwTotal" parameterType="String" resultType="Long">
        select count(*) from flw_table where userId=#{userId}
    </select>

    <select id="findByUserId" resultType="User">
        select * from user_table where user_table.id in
                                       (select flwName from flw_table where flw_table.userId=#{userId})
                           order by 1 desc
        Limit #{page},#{size}
    </select>
</mapper>